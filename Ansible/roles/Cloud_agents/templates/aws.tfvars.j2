
aws_bucket_tfstate = "{{ aws_bucket_tfstate }}"
aws_region_tfstate = "{{ aws_region_tfstate }}"
aws_bucketkey_tfstate = "{{ aws_bucketkey_tfstate }}"

aws_access_key = "{{ aws_access_key }}"
aws_secret_key = "{{ aws_secret_key }}"
aws_region = "{{ aws_region }}"

aws_vpc_name = "{{ aws_vpc_name }}"
aws_vpc_cidr = "{{ aws_vpc_cidr }}"

aws_sg_name = "{{ aws_sg_name }}"
aws_sg_desc = "{{ aws_sg_desc }}"